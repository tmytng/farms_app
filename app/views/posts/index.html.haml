.contents
  = render "layouts/header"
  .main
    = render "layouts/pj-category"
    .contact-field
      %table.table.table-striped.table-hover
        %thead
          %tr
            %th 問い合わせ番号
            %th 会社名
            %th 担当者
            %th ステータス
            %th 問い合わせ日時
            %th 更新日時
            %th 登録者
        %tbody
        - @posts.each do |post|
          %tr
            %td
              = link_to project_post_path(@project, post) do
                #{post.id}
            %td #{post.company_name}
            %td #{post.contact_person}
            %td #{post.leadstatus.name}
            %td #{post.created_at.to_s(:datetime)}
            %td #{post.updated_at.to_s(:datetime)}
            %td #{post.user.name}
= link_to new_project_post_path do
  .postbtn
    %span.postbtn__text 登録する
    = image_tag 'icon/icon_camera.png', alt: '出品ボタン', class: 'postbtn__icon'
