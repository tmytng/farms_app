.pj-contents
  = render "layouts/global-navigation"
  = render "layouts/pj-header"
  .pj-main
    .contact-field
      %table.table.table-striped.table-hover
        %thead
          %tr
            %th 問い合わせ番号
            %th 会社名
            %th 担当者
            %th ステータス
            %th 問い合わせ日時
            %th 更新日時
            %th 登録者
        %tbody
        - @posts.each do |post|
          %tr
            %td
              = link_to project_post_path(@project, post) do
                #{post.id}
            %td #{post.company_name}
            %td #{post.contact_person}
            %td #{post.leadstatus.name}
            %td #{post.created_at.strftime("%Y年%m月%d日 %H時%M分")}
            %td #{post.updated_at.strftime("%Y年%m月%d日 %H時%M分")}
            %td #{post.user.name}
= link_to new_project_post_path do
  .postbtn
    %span.postbtn__text 新規登録
    = image_tag 'icon/icon_pen.png', alt: '出品ボタン', class: 'postbtn__icon'
