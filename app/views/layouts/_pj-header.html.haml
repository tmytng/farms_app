.jumbotron.mb-0.py-2
  .row
    .col-md-12
      .pj-sub
        - if request.path_info == project_path(@project)
          = "メッセージボード：#{@project.name}"
        - else
          = @project.name
  .row
    .col-md-12
      .pj-member
        %ul.member-list
          members:
          - @project.users.each do |member|
            %li.member-icons
              - if member.avatar.attached?
                = link_to user_path(member) do
                  = image_tag member.avatar
  .row.pb-2
    .col-md-2
    .col-md-8
      .pj-desc
        .desc-box
          = @project.profile
    .col-md-2.pj-sidebtn
      - if request.path_info == project_path(@project)
        = link_to "コンタクトログへ", project_posts_path(@project), class: 'btn btn-primary btn-sm'
      - else
        = link_to "メッセージボードへ", project_path(@project), class: 'btn btn-primary btn-sm'
